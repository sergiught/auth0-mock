{{- /* Render `go-licenses report` as a Markdown summary suitable for shipping
       alongside release archives. The CSV-style default output isn't
       reader-friendly; this layout is. Pair each module with its license
       SPDX id and a link to the upstream LICENSE file.
*/ -}}
# Third-party software in auth0-mock

This binary statically links the Go modules listed below. Each is used under
the license noted in its row; the full license text travels with the archive
in the `licenses/` directory next to this file.

| Module | License |
|---|---|
{{- range . }}
| `{{ .Name }}` | [{{ .LicenseName }}]({{ .LicenseURL }}) |
{{- end }}
